/**
 * Renderer preload script.
 *
 * Intentionally empty for now — the renderer talks to Fastify over HTTP/WS
 * and needs no bridge to the main process. Kept as a placeholder for future
 * native integrations (notifications, deep links, system tray).
 */
export {};
